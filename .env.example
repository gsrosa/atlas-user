# Atlas BFF (tRPC) — same host as atlas-shell VITE_API_URL
VITE_API_URL=http://localhost:4000

# Optional: absolute origin for /locales (defaults to import.meta.url origin; set if asset URLs break)
# VITE_I18N_ASSET_ORIGIN=http://localhost:3003

# Supabase — Project Settings → API (browser-safe anon key only)
VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
